## Instructions to use:
- Put these files in the .vscode directory of the project. 
- Compile with Ctrl+Shift+B.
- The output executable files will appear in the bin folder, from where they can be executed
- Before trying to compile, make sure you have installed libevdev-devel and raylib to ensure there are no errors 